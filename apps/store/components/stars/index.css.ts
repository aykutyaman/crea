import { sprinkles } from '@crea/design';

export const filled = sprinkles({
  color: 'yellow-500',
});

export const list = sprinkles({
  display: 'flex',
  marginTop: '3x',
  marginBottom: '3x',
});
